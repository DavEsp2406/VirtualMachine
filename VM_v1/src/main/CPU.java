package main;

public class CPU {
/*
 * tiene una pila(array) de tipo OPERANDSTACK y tambien tiene un atributo que se llama memoria
 * de tipo Memory y tambien tiene un boolean que sera la condicion de parada, una constructora
 * , tambien tiene un metodo toString el cual hará llamada al metodo toString de OperandStack
 * y al de memory. Tienen un metodo llamado erase el cual borra la pila y la memoria, tiene otro metodo
 * que retorna un booleano que dice si esta vacio o no, como empty (hall).
 * sumaPila, restaPila, multiplicaPila, dividePila (4 metodos), hay que tener en cuenta
 * varios inconvenientes ya que se puede dividir entre 0, sumar solo un numero etc..
 * 
 * sumaPila se puede hacer con pop, pop y push. Si no se puede hay que volver a meter el numero 
 * y dar un mensaje de error
 */
}
